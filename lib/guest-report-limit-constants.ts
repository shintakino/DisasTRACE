/** Browser-safe limits shared by admin settings and server-side Guest Mode enforcement. */
export const DEFAULT_GUEST_REPORTS_PER_PHONE_LIMIT = 3;
export const MAX_GUEST_REPORTS_PER_PHONE_LIMIT = 100;
